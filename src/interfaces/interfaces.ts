export interface PageTransitionProps {
  children: React.ReactNode;
}

export interface SocialProps {
  containerStyles: string;
  iconStyles: string;
}

export interface WorkSliderBtnsProps {
  cointainerStyles: string;
  btnStyles: string;
}